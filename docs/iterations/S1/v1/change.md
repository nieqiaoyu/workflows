# S1 v1 需求变更

## 变动人

- 姓名：模拟产品
- 角色：产品
- 时间：2026-06-26 10:10:00

## 变更说明

- 订单展示中的创建人字段由 `createId` 调整为 `createName`。
- 前端展示创建人姓名，不再展示创建人账号 ID。
- 后端接口需要确认订单列表和订单详情页都返回 `createName`。

## 影响范围

- 影响模块：订单展示
- 影响接口：订单列表接口、订单详情接口
- 影响页面：订单列表页、订单详情页

## 需要关注

- 前端：将订单展示字段从 `createId` 替换为 `createName`。
- 后端：确认接口返回 `createName`，并兼容字段来源。
- 测试：覆盖订单列表和订单详情页创建人展示。
