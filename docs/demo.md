# Demo 文档

这是用于验证文档更新自动化 MVP 的测试文档。

## API 变更

- 新增 `/demo/status` 接口，用于验证自动摘要流程。
- 响应字段新增 `status`，取值为 `ok` 或 `failed`。

## 需要关注

- 如果业务代码已有 demo 健康检查逻辑，需要确认字段命名是否一致。
- 本次更新用于验证自动递增文档变更版本号。
- 本次更新用于验证时间格式统一为 yyyy-MM-dd HH:mm:ss。
- 本次更新用于验证企业微信机器人通知。
- 本次更新用于验证企业微信手机号 @ 负责人。
- 本次更新用于诊断企业微信手机号 @ 展示效果。
- 本次更新用于验证 MODULE_OWNERS_JSON 标准 JSON 配置。
- 本次更新用于验证每次文档变化固定 @ 测试角色。
- 本次更新用于验证企微提醒文本不显示手机号尾号。
- 本次更新用于验证企微提醒文本显示 @沈阳osshenyang。
